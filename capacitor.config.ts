import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'space.eatit.app',
  appName: 'Homie',
  webDir: 'dist/eat_it_ng/browser',
};

export default config;
